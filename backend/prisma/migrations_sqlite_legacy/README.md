# Legacy SQLite Migrations

This folder contains the pre-Clerk Prisma migration history that was generated for SQLite.

These migrations are kept for historical reference only. They are not part of the active migration chain anymore.

The active Prisma migration history now lives in `backend/prisma/migrations/` and starts from the Postgres baseline used for Supabase.
